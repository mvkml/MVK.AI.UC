# Dev .NET Agent — Work Log
## Date: 2026-06-30
## Time: 14:00:00
## Subject: jwt_login

### What Was Done
- Implemented JWT access token issuance on `POST api/users/login` per architect-discussed plan: user chose **Basic JWT only, no DB change** (deferred refresh-token persistence).
- Added `JwtSettings`, `ITokenService`/`TokenService` (`AI.HR.BL/Security/`) — generates signed HMAC-SHA256 JWT with `sub`/`email`/role claims.
- `LoginResponse.Token` (new property) carries the JWT back to the client.
- `UserBL.Login` now constructor-injects `ITokenService`, calls `GenerateToken` after password verification succeeds.
- `Program.cs`: added `Jwt` config section to `appsettings.json` (Issuer/Audience/SecretKey/ExpiryMinutes), registered `AddAuthentication().AddJwtBearer(...)`, `AddAuthorization()`, `app.UseAuthentication()` before `UseAuthorization()`.
- Packages: `System.IdentityModel.Tokens.Jwt` (AI.HR.BL), `Microsoft.AspNetCore.Authentication.JwtBearer` pinned to `8.0.28` (AI.HR.Api) — the unpinned `dotnet add package` pulled `10.0.9` which only targets `net10.0` and failed to restore against this solution's `net8.0`.
- Rebuilt (0 errors/warnings), restarted API on port 5008, verified live: Login returns a `token`, decoded payload confirmed correct claims, `GET api/users/roles` (no auth) still works unaffected by the new middleware.
- Updated `api_data.js` dashboard, `REST_API_ENDPOINTS.md`, created `TDD_users_signup/004_TDD_users_login.md`.

### Decisions Made
- User explicitly chose Basic JWT (no refresh tokens, no DB table) after being asked — recorded as a real architectural decision, not a default assumption.
- Dev `SecretKey` committed in `appsettings.json` for now — flagged as needing to move to a secrets manager before QA/UAT/Prod exist, but acceptable for single-developer Dev work today.

### Pending / Next Steps
- No `[Authorize]` endpoints exist yet — nothing actually validates the token server-side in practice until a protected endpoint is built.
- Move `Jwt:SecretKey` to a secrets manager before any non-Dev environment is provisioned.
- Wire `login.ts`/`signup.ts` (Angular) to call the real endpoints and store the token.
